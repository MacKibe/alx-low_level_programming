#include "main.h"
/**
 * _isdigit - writes the character c to stdout
 * @c: the character being checked
 * Return: 1 if digit else 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= 9)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
