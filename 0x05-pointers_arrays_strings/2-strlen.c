#include "main.h"
/**
  *_strlen - returns lenght of string
  *s is string
  *returns length of string
  */
int _strlen(char *s)
{
 int length - 0;
  
  while(*s++)
    length++;
  
  return(length);
}
